<h1>我的博客</h1>
  <% @microposts.each do |m| %>
    <%= link_to m.title, micropost_path(m.id) %><br/>
    <span class="timestamp">
      Posted <%= time_ago_in_words(m.created_at) %> ago.
    </span><hr/>    
  <% end %>
